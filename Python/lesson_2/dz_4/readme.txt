# Сделал генератор по выборки значения из списка
# И вывожу  строку от значения после последнего пробела до конца строки
# А функцией .capitalize() перевожу все к одному регистру и поднимаю первую букву
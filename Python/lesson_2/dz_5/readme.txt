# Для разделения на рубли и коппейки использую по нахождению точки (.count('.') == 1)
  и если коппейка одна (572.8) то значит это 80 коппеек, а значит добавляем 0.
# Сортирую по возрастанию .sort и показываю что id не изменился
# Сортирую по убыванию sorted(list_price,reverse = True) создавая новый обьект
# И сортирую по возрастанию и срезом вывожу пять самых дорогих товаров

Также в dz_5_d  еще варианты сортировки для подпункта d 
